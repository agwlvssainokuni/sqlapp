<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:jee="http://www.springframework.org/schema/jee"
	xsi:schemaLocation="http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-4.0.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd">

	<jee:jndi-lookup id="dataSource" jndi-name="${sqlapp.jdbc.dataSource}" />

	<bean id="basicDataSource" class="org.apache.commons.dbcp.BasicDataSource"
		destroy-method="close">
		<property name="driverClassName" value="${sqlapp.jdbc.driverClassName}" />
		<property name="url" value="${sqlapp.jdbc.url}" />
		<property name="username" value="${sqlapp.jdbc.username}" />
		<property name="password" value="${sqlapp.jdbc.password}" />
	</bean>

	<bean id="transactionManager"
		class="org.springframework.transaction.jta.JtaTransactionManager">
		<property name="userTransactionName" value="${sqlapp.jta.userTransaction}" />
		<property name="transactionManagerName" value="${sqlapp.jta.transactionManager}" />
		<property name="transactionSynchronizationRegistryName"
			value="${sqlapp.jta.transactionSynchronizationRegistry}" />
	</bean>

</beans>
