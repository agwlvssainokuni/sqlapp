<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cherry.sqlapp.db.app.mapper.LoadMapper">
	<insert id="create" parameterType="cherry.sqlapp.db.gen.dto.SqlCsv">
		INSERT INTO sql_csv (
			id,
			database_name,
			query
		)
		VALUES (
			#{id,jdbcType=INTEGER},
			#{databaseName,jdbcType=VARCHAR},
			#{query,jdbcType=VARCHAR}
		)
	</insert>
	<update id="update" parameterType="cherry.sqlapp.db.gen.dto.SqlCsv">
		UPDATE sql_csv
		SET
			database_name = #{databaseName,jdbcType=VARCHAR},
			query = #{query,jdbcType=VARCHAR},
			updated_at = CURRENT_TIMESTAMP
		WHERE
			id = #{id,jdbcType=INTEGER}
			AND
			deleted_flg = 0
	</update>
</mapper>
